#pragma once

#include "types.h"

namespace Cmn {
    namespace Def {
        enum WeaponKind {
            cNone = -1,
            cMain = 0,
            cSub = 1,
            cSpecial = 2
        };
    }; // namespace Def
}; // namespace Cmn